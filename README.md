# MyTorch
A custom PyTorch-like deep learning framework using only NumPy, implementing core features such as convolutional layers, recurrent units, optimizers, loss functions, batch normalization, dropout, and an autograd engine for automatic differentiation.
